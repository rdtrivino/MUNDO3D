<?php

// autoload.php @generated by Composer

// Verifica si la versión de PHP es menor a 5.6
if (PHP_VERSION_ID < 50600) {
    // Si los encabezados no se han enviado, envía un encabezado de error 500
    if (!headers_sent()) {
        header('HTTP/1.1 500 Internal Server Error');
    }
    // Mensaje de error indicando que Composer 2.3.0 no es compatible con PHP < 5.6
    $err = 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running '.PHP_VERSION.', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.'.PHP_EOL;
    
    // Verifica si la directiva `display_errors` está desactivada
    if (!ini_get('display_errors')) {
        // Si la ejecución es en CLI o phpdbg, muestra el mensaje en STDERR
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, $err);
        // Si no se han enviado encabezados, muestra el mensaje como salida estándar
        } elseif (!headers_sent()) {
            echo $err;
        }
    }
    // Dispara un error de usuario con el mensaje de error
    trigger_error(
        $err,
        E_USER_ERROR
    );
}

// Incluye el archivo del cargador automático de Composer
require_once __DIR__ . '/composer/autoload_real.php';

// Devuelve el cargador inicializado por Composer
return ComposerAutoloaderInit463aacefda2fa947d5b31a599cd6e495::getLoader();

