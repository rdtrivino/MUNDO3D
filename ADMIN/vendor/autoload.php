<?php

// autoload.php @generated by Composer
// Este archivo fue generado automáticamente por Composer. 
// Su propósito es cargar automáticamente las dependencias y clases necesarias de los paquetes configurados en Composer.

// Verifica si la versión de PHP es menor que 5.6.
if (PHP_VERSION_ID < 50600) {
    // Si las cabeceras HTTP aún no se han enviado, devuelve un error HTTP 500.
    if (!headers_sent()) {
        header('HTTP/1.1 500 Internal Server Error');
    }
    
    // Construye un mensaje de error indicando que Composer 2.3.0 no es compatible con PHP <5.6.
    $err = 'Composer 2.3.0 dropped support for autoloading on PHP <5.6 and you are running '.PHP_VERSION.', please upgrade PHP or use Composer 2.2 LTS via "composer self-update --2.2". Aborting.'.PHP_EOL;
    
    // Comprueba si la configuración `display_errors` está desactivada en PHP.
    if (!ini_get('display_errors')) {
        // Si el script se ejecuta desde la línea de comandos (CLI) o el depurador PHP (phpdbg), escribe el error en la salida estándar (STDERR).
        if (PHP_SAPI === 'cli' || PHP_SAPI === 'phpdbg') {
            fwrite(STDERR, $err);
        } 
        // Si no se han enviado las cabeceras, muestra el error como salida.
        elseif (!headers_sent()) {
            echo $err;
        }
    }
    
    // Dispara un error fatal (E_USER_ERROR) para detener la ejecución del script.
    trigger_error(
        $err,
        E_USER_ERROR
    );
}

// Incluye el archivo `autoload_real.php`, que contiene la lógica principal del autoloading.
// Este archivo es generado por Composer y gestiona la configuración del autoloader.
require_once __DIR__ . '/composer/autoload_real.php';

// Devuelve el cargador automático generado por Composer.
// La clase `ComposerAutoloaderInitac2d307f4ed514763a8119f3523066bb` es una clase única generada dinámicamente para inicializar el autoloading.
return ComposerAutoloaderInitac2d307f4ed514763a8119f3523066bb::getLoader();
